'use client';
import { useState } from 'react';
import './admin.css';
const initial=['Website','Control Panel','Minecraft Hosting','VPS Hosting','Payment Gateway','Mail Hosting','API','Discord Bot'];
export default function Admin(){const [services,setServices]=useState(initial);const [name,setName]=useState('');const [msg,setMsg]=useState('');function add(){if(!name.trim())return;setServices([...services,name.trim()]);setName('');setMsg('Service added locally. Connect Supabase to persist changes.')}return <main className="adminPage"><nav className="nav"><div className="brand"><span className="brandMark">A</span><span>ANIME <b>CLOUD</b></span></div><a href="/" style={{color:'#aaa',textDecoration:'none'}}>← Public status</a></nav><section className="adminWrap"><p className="eyebrow">ADMIN CONSOLE</p><h1>Status management</h1><p className="muted">Manage services and incidents. This starter console is intentionally secret-free; production authentication and database persistence should be configured with Supabase.</p><div className="adminCard"><h2>Services</h2>{services.map(s=><div className="row" key={s}><span>{s}</span><select defaultValue="operational"><option value="operational">Operational</option><option value="degraded">Degraded</option><option value="outage">Major outage</option></select></div>)}<div className="add"><input value={name} onChange={e=>setName(e.target.value)} placeholder="New service name"/><button onClick={add}>Add service</button></div>{msg&&<p className="success">{msg}</p>}</div><div className="adminCard"><h2>Incidents</h2><p className="muted">Create, update and resolve incidents from this panel after connecting your database.</p><button className="primary" onClick={()=>setMsg('Incident editor ready — configure Supabase persistence to enable publishing.')}>Create incident</button></div></section></main>}